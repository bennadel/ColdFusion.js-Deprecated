

// I am the request proxy constructor.
function RequestProxy( request, cookies ){
	// Nothing to proxy yet???
}


// ---------------------------------------------------------- //
// ---------------------------------------------------------- //


// Export an instance factory.
exports.newInstance = function( request, cookies ){

	// For now, just store the cookies in the request and return
	// the actual request object.
	request.cookies = cookies;

	// Return the raw request - no idea if we really need a proxy.
	return( request );
	
};

